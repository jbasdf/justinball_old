box: node
build:
  steps:
    - npm-install
    - script:
        name: run build
        code: |
          echo "npm run build"
deploy:
  steps:
    - s3sync:
        key_id: $KEY
        key_secret: $SECRET
        bucket_url: $BUCKET_URL
        source_dir: build/prod